name: Aceiro fix

on:
    issue_comment:
        types: [created]

permissions: {}

jobs:
    fix:
        if: >-
            github.event.issue.pull_request
            && contains(github.event.comment.body, '/fix ')
        permissions:
            contents: write
            pull-requests: write
            id-token: write
            actions: read
        uses: svozza/aceiro/.github/workflows/ai-pr-fix.yml@bcb11084a005e3e1db9587a3a32eb2a857825b02
        with:
            project-description: "ferrumio/rboto, Async, strictly typed AWS clients for Python, powered by the official AWS SDK for Rust."
            use-bedrock-api-key: true
        secrets:
            BEDROCK_API_KEY: ${{ secrets.BEDROCK_API_KEY }}
