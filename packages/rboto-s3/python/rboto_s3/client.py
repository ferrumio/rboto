# Generated by rboto-codegen. DO NOT EDIT.
from __future__ import annotations



from ._native import S3Client as NativeClient, AbortMultipartUploadOutput, CompleteMultipartUploadOutput, CopyObjectOutput, CreateBucketOutput, CreateMultipartUploadOutput, CreateSessionOutput, DeleteObjectOutput, DeleteObjectTaggingOutput, DeleteObjectsOutput, GetBucketAbacOutput, GetBucketAccelerateConfigurationOutput, GetBucketAclOutput, GetBucketAnalyticsConfigurationOutput, GetBucketCorsOutput, GetBucketEncryptionOutput, GetBucketIntelligentTieringConfigurationOutput, GetBucketInventoryConfigurationOutput, GetBucketLifecycleConfigurationOutput, GetBucketLocationOutput, GetBucketLoggingOutput, GetBucketMetadataConfigurationOutput, GetBucketMetadataTableConfigurationOutput, GetBucketMetricsConfigurationOutput, GetBucketNotificationConfigurationOutput, GetBucketOwnershipControlsOutput, GetBucketPolicyOutput, GetBucketPolicyStatusOutput, GetBucketReplicationOutput, GetBucketRequestPaymentOutput, GetBucketTaggingOutput, GetBucketVersioningOutput, GetBucketWebsiteOutput, GetObjectAclOutput, GetObjectAttributesOutput, GetObjectLegalHoldOutput, GetObjectLockConfigurationOutput, GetObjectOutput, GetObjectRetentionOutput, GetObjectTaggingOutput, GetObjectTorrentOutput, GetPublicAccessBlockOutput, HeadBucketOutput, HeadObjectOutput, ListBucketAnalyticsConfigurationsOutput, ListBucketIntelligentTieringConfigurationsOutput, ListBucketInventoryConfigurationsOutput, ListBucketMetricsConfigurationsOutput, ListBucketsOutput, ListDirectoryBucketsOutput, ListMultipartUploadsOutput, ListObjectVersionsOutput, ListObjectsOutput, ListObjectsV2Output, ListPartsOutput, PutBucketLifecycleConfigurationOutput, PutObjectAclOutput, PutObjectLegalHoldOutput, PutObjectLockConfigurationOutput, PutObjectOutput, PutObjectRetentionOutput, PutObjectTaggingOutput, RenameObjectOutput, RestoreObjectOutput, UploadPartCopyOutput, UploadPartOutput
from .types import (
    AbacStatus,
    AccelerateConfiguration,
    AccessControlPolicy,
    AnalyticsConfiguration,
    BucketCannedACL,
    BucketLifecycleConfiguration,
    BucketLoggingStatus,
    CORSConfiguration,
    ChecksumAlgorithm,
    ChecksumMode,
    ChecksumType,
    CompletedMultipartUpload,
    CreateBucketConfiguration,
    Delete,
    EncodingType,
    ExpressionType,
    InputSerialization,
    IntelligentTieringConfiguration,
    InventoryConfiguration,
    InventoryTableConfigurationUpdates,
    JournalTableConfigurationUpdates,
    MetadataConfiguration,
    MetadataDirective,
    MetadataTableConfiguration,
    MetricsConfiguration,
    NotificationConfiguration,
    ObjectAttributes,
    ObjectCannedACL,
    ObjectLockConfiguration,
    ObjectLockLegalHold,
    ObjectLockLegalHoldStatus,
    ObjectLockMode,
    ObjectLockRetention,
    ObjectOwnership,
    OptionalObjectAttributes,
    OutputSerialization,
    OwnershipControls,
    PublicAccessBlockConfiguration,
    ReplicationConfiguration,
    ReplicationStatus,
    RequestCharged,
    RequestPayer,
    RequestPaymentConfiguration,
    RequestProgress,
    RestoreRequest,
    ScanRange,
    SelectObjectContentOutput,
    ServerSideEncryption,
    ServerSideEncryptionConfiguration,
    SessionMode,
    StorageClass,
    Tagging,
    TaggingDirective,
    TransitionDefaultMinimumObjectSize,
    VersioningConfiguration,
    WebsiteConfiguration,
)


class S3Client:
    def __init__(
        self,
        *,
        region: str | None = None,
        profile: str | None = None,
        endpoint_url: str | None = None,
    ) -> None:
        self._native = NativeClient(
            region=region,
            profile=profile,
            endpoint_url=endpoint_url,
        )


    async def abort_multipart_upload(
        self,
        *,
        bucket: str,
        key: str,
        upload_id: str,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        if_match_initiated_time: str | None = None,
    ) -> AbortMultipartUploadOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "upload_id": upload_id,
        }
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if if_match_initiated_time is not None:
            params["if_match_initiated_time"] = if_match_initiated_time
        result = await self._native.abort_multipart_upload(params)
        return result


    async def complete_multipart_upload(
        self,
        *,
        bucket: str,
        key: str,
        upload_id: str,
        multipart_upload: CompletedMultipartUpload | None = None,
        checksum_crc32: str | None = None,
        checksum_crc32_c: str | None = None,
        checksum_crc64_nvme: str | None = None,
        checksum_sha1: str | None = None,
        checksum_sha256: str | None = None,
        checksum_type: ChecksumType | None = None,
        mpu_object_size: int | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        if_match: str | None = None,
        if_none_match: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
    ) -> CompleteMultipartUploadOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "upload_id": upload_id,
        }
        if multipart_upload is not None:
            params["multipart_upload"] = multipart_upload
        if checksum_crc32 is not None:
            params["checksum_crc32"] = checksum_crc32
        if checksum_crc32_c is not None:
            params["checksum_crc32_c"] = checksum_crc32_c
        if checksum_crc64_nvme is not None:
            params["checksum_crc64_nvme"] = checksum_crc64_nvme
        if checksum_sha1 is not None:
            params["checksum_sha1"] = checksum_sha1
        if checksum_sha256 is not None:
            params["checksum_sha256"] = checksum_sha256
        if checksum_type is not None:
            params["checksum_type"] = checksum_type
        if mpu_object_size is not None:
            params["mpu_object_size"] = mpu_object_size
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if if_match is not None:
            params["if_match"] = if_match
        if if_none_match is not None:
            params["if_none_match"] = if_none_match
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        result = await self._native.complete_multipart_upload(params)
        return result


    async def copy_object(
        self,
        *,
        bucket: str,
        copy_source: str,
        key: str,
        acl: ObjectCannedACL | None = None,
        cache_control: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        content_disposition: str | None = None,
        content_encoding: str | None = None,
        content_language: str | None = None,
        content_type: str | None = None,
        copy_source_if_match: str | None = None,
        copy_source_if_modified_since: str | None = None,
        copy_source_if_none_match: str | None = None,
        copy_source_if_unmodified_since: str | None = None,
        expires: str | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write_acp: str | None = None,
        if_match: str | None = None,
        if_none_match: str | None = None,
        metadata: dict[str, str] | None = None,
        metadata_directive: MetadataDirective | None = None,
        tagging_directive: TaggingDirective | None = None,
        server_side_encryption: ServerSideEncryption | None = None,
        storage_class: StorageClass | None = None,
        website_redirect_location: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        ssekms_key_id: str | None = None,
        ssekms_encryption_context: str | None = None,
        bucket_key_enabled: bool | None = None,
        copy_source_sse_customer_algorithm: str | None = None,
        copy_source_sse_customer_key: str | None = None,
        copy_source_sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        tagging: str | None = None,
        object_lock_mode: ObjectLockMode | None = None,
        object_lock_retain_until_date: str | None = None,
        object_lock_legal_hold_status: ObjectLockLegalHoldStatus | None = None,
        expected_bucket_owner: str | None = None,
        expected_source_bucket_owner: str | None = None,
    ) -> CopyObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "copy_source": copy_source,
            "key": key,
        }
        if acl is not None:
            params["acl"] = acl
        if cache_control is not None:
            params["cache_control"] = cache_control
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if content_disposition is not None:
            params["content_disposition"] = content_disposition
        if content_encoding is not None:
            params["content_encoding"] = content_encoding
        if content_language is not None:
            params["content_language"] = content_language
        if content_type is not None:
            params["content_type"] = content_type
        if copy_source_if_match is not None:
            params["copy_source_if_match"] = copy_source_if_match
        if copy_source_if_modified_since is not None:
            params["copy_source_if_modified_since"] = copy_source_if_modified_since
        if copy_source_if_none_match is not None:
            params["copy_source_if_none_match"] = copy_source_if_none_match
        if copy_source_if_unmodified_since is not None:
            params["copy_source_if_unmodified_since"] = copy_source_if_unmodified_since
        if expires is not None:
            params["expires"] = expires
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if if_match is not None:
            params["if_match"] = if_match
        if if_none_match is not None:
            params["if_none_match"] = if_none_match
        if metadata is not None:
            params["metadata"] = metadata
        if metadata_directive is not None:
            params["metadata_directive"] = metadata_directive
        if tagging_directive is not None:
            params["tagging_directive"] = tagging_directive
        if server_side_encryption is not None:
            params["server_side_encryption"] = server_side_encryption
        if storage_class is not None:
            params["storage_class"] = storage_class
        if website_redirect_location is not None:
            params["website_redirect_location"] = website_redirect_location
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if ssekms_key_id is not None:
            params["ssekms_key_id"] = ssekms_key_id
        if ssekms_encryption_context is not None:
            params["ssekms_encryption_context"] = ssekms_encryption_context
        if bucket_key_enabled is not None:
            params["bucket_key_enabled"] = bucket_key_enabled
        if copy_source_sse_customer_algorithm is not None:
            params["copy_source_sse_customer_algorithm"] = copy_source_sse_customer_algorithm
        if copy_source_sse_customer_key is not None:
            params["copy_source_sse_customer_key"] = copy_source_sse_customer_key
        if copy_source_sse_customer_key_md5 is not None:
            params["copy_source_sse_customer_key_md5"] = copy_source_sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if tagging is not None:
            params["tagging"] = tagging
        if object_lock_mode is not None:
            params["object_lock_mode"] = object_lock_mode
        if object_lock_retain_until_date is not None:
            params["object_lock_retain_until_date"] = object_lock_retain_until_date
        if object_lock_legal_hold_status is not None:
            params["object_lock_legal_hold_status"] = object_lock_legal_hold_status
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if expected_source_bucket_owner is not None:
            params["expected_source_bucket_owner"] = expected_source_bucket_owner
        result = await self._native.copy_object(params)
        return result


    async def create_bucket(
        self,
        *,
        bucket: str,
        acl: BucketCannedACL | None = None,
        create_bucket_configuration: CreateBucketConfiguration | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write: str | None = None,
        grant_write_acp: str | None = None,
        object_lock_enabled_for_bucket: bool | None = None,
        object_ownership: ObjectOwnership | None = None,
    ) -> CreateBucketOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if acl is not None:
            params["acl"] = acl
        if create_bucket_configuration is not None:
            params["create_bucket_configuration"] = create_bucket_configuration
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write is not None:
            params["grant_write"] = grant_write
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if object_lock_enabled_for_bucket is not None:
            params["object_lock_enabled_for_bucket"] = object_lock_enabled_for_bucket
        if object_ownership is not None:
            params["object_ownership"] = object_ownership
        result = await self._native.create_bucket(params)
        return result


    async def create_bucket_metadata_configuration(
        self,
        *,
        bucket: str,
        metadata_configuration: MetadataConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "metadata_configuration": metadata_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.create_bucket_metadata_configuration(params)
        return None


    async def create_bucket_metadata_table_configuration(
        self,
        *,
        bucket: str,
        metadata_table_configuration: MetadataTableConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "metadata_table_configuration": metadata_table_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.create_bucket_metadata_table_configuration(params)
        return None


    async def create_multipart_upload(
        self,
        *,
        bucket: str,
        key: str,
        acl: ObjectCannedACL | None = None,
        cache_control: str | None = None,
        content_disposition: str | None = None,
        content_encoding: str | None = None,
        content_language: str | None = None,
        content_type: str | None = None,
        expires: str | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write_acp: str | None = None,
        metadata: dict[str, str] | None = None,
        server_side_encryption: ServerSideEncryption | None = None,
        storage_class: StorageClass | None = None,
        website_redirect_location: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        ssekms_key_id: str | None = None,
        ssekms_encryption_context: str | None = None,
        bucket_key_enabled: bool | None = None,
        request_payer: RequestPayer | None = None,
        tagging: str | None = None,
        object_lock_mode: ObjectLockMode | None = None,
        object_lock_retain_until_date: str | None = None,
        object_lock_legal_hold_status: ObjectLockLegalHoldStatus | None = None,
        expected_bucket_owner: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        checksum_type: ChecksumType | None = None,
    ) -> CreateMultipartUploadOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if acl is not None:
            params["acl"] = acl
        if cache_control is not None:
            params["cache_control"] = cache_control
        if content_disposition is not None:
            params["content_disposition"] = content_disposition
        if content_encoding is not None:
            params["content_encoding"] = content_encoding
        if content_language is not None:
            params["content_language"] = content_language
        if content_type is not None:
            params["content_type"] = content_type
        if expires is not None:
            params["expires"] = expires
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if metadata is not None:
            params["metadata"] = metadata
        if server_side_encryption is not None:
            params["server_side_encryption"] = server_side_encryption
        if storage_class is not None:
            params["storage_class"] = storage_class
        if website_redirect_location is not None:
            params["website_redirect_location"] = website_redirect_location
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if ssekms_key_id is not None:
            params["ssekms_key_id"] = ssekms_key_id
        if ssekms_encryption_context is not None:
            params["ssekms_encryption_context"] = ssekms_encryption_context
        if bucket_key_enabled is not None:
            params["bucket_key_enabled"] = bucket_key_enabled
        if request_payer is not None:
            params["request_payer"] = request_payer
        if tagging is not None:
            params["tagging"] = tagging
        if object_lock_mode is not None:
            params["object_lock_mode"] = object_lock_mode
        if object_lock_retain_until_date is not None:
            params["object_lock_retain_until_date"] = object_lock_retain_until_date
        if object_lock_legal_hold_status is not None:
            params["object_lock_legal_hold_status"] = object_lock_legal_hold_status
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if checksum_type is not None:
            params["checksum_type"] = checksum_type
        result = await self._native.create_multipart_upload(params)
        return result


    async def create_session(
        self,
        *,
        bucket: str,
        session_mode: SessionMode | None = None,
        server_side_encryption: ServerSideEncryption | None = None,
        ssekms_key_id: str | None = None,
        ssekms_encryption_context: str | None = None,
        bucket_key_enabled: bool | None = None,
    ) -> CreateSessionOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if session_mode is not None:
            params["session_mode"] = session_mode
        if server_side_encryption is not None:
            params["server_side_encryption"] = server_side_encryption
        if ssekms_key_id is not None:
            params["ssekms_key_id"] = ssekms_key_id
        if ssekms_encryption_context is not None:
            params["ssekms_encryption_context"] = ssekms_encryption_context
        if bucket_key_enabled is not None:
            params["bucket_key_enabled"] = bucket_key_enabled
        result = await self._native.create_session(params)
        return result


    async def delete_bucket(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket(params)
        return None


    async def delete_bucket_analytics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_analytics_configuration(params)
        return None


    async def delete_bucket_cors(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_cors(params)
        return None


    async def delete_bucket_encryption(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_encryption(params)
        return None


    async def delete_bucket_intelligent_tiering_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_intelligent_tiering_configuration(params)
        return None


    async def delete_bucket_inventory_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_inventory_configuration(params)
        return None


    async def delete_bucket_lifecycle(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_lifecycle(params)
        return None


    async def delete_bucket_metadata_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_metadata_configuration(params)
        return None


    async def delete_bucket_metadata_table_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_metadata_table_configuration(params)
        return None


    async def delete_bucket_metrics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_metrics_configuration(params)
        return None


    async def delete_bucket_ownership_controls(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_ownership_controls(params)
        return None


    async def delete_bucket_policy(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_policy(params)
        return None


    async def delete_bucket_replication(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_replication(params)
        return None


    async def delete_bucket_tagging(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_tagging(params)
        return None


    async def delete_bucket_website(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_bucket_website(params)
        return None


    async def delete_object(
        self,
        *,
        bucket: str,
        key: str,
        mfa: str | None = None,
        version_id: str | None = None,
        request_payer: RequestPayer | None = None,
        bypass_governance_retention: bool | None = None,
        expected_bucket_owner: str | None = None,
        if_match: str | None = None,
        if_match_last_modified_time: str | None = None,
        if_match_size: int | None = None,
    ) -> DeleteObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if mfa is not None:
            params["mfa"] = mfa
        if version_id is not None:
            params["version_id"] = version_id
        if request_payer is not None:
            params["request_payer"] = request_payer
        if bypass_governance_retention is not None:
            params["bypass_governance_retention"] = bypass_governance_retention
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if if_match is not None:
            params["if_match"] = if_match
        if if_match_last_modified_time is not None:
            params["if_match_last_modified_time"] = if_match_last_modified_time
        if if_match_size is not None:
            params["if_match_size"] = if_match_size
        result = await self._native.delete_object(params)
        return result


    async def delete_objects(
        self,
        *,
        bucket: str,
        delete: Delete,
        mfa: str | None = None,
        request_payer: RequestPayer | None = None,
        bypass_governance_retention: bool | None = None,
        expected_bucket_owner: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
    ) -> DeleteObjectsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "delete": delete,
        }
        if mfa is not None:
            params["mfa"] = mfa
        if request_payer is not None:
            params["request_payer"] = request_payer
        if bypass_governance_retention is not None:
            params["bypass_governance_retention"] = bypass_governance_retention
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        result = await self._native.delete_objects(params)
        return result


    async def delete_object_tagging(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> DeleteObjectTaggingOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.delete_object_tagging(params)
        return result


    async def delete_public_access_block(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.delete_public_access_block(params)
        return None


    async def get_bucket_abac(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketAbacOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_abac(params)
        return result


    async def get_bucket_accelerate_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
        request_payer: RequestPayer | None = None,
    ) -> GetBucketAccelerateConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if request_payer is not None:
            params["request_payer"] = request_payer
        result = await self._native.get_bucket_accelerate_configuration(params)
        return result


    async def get_bucket_acl(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketAclOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_acl(params)
        return result


    async def get_bucket_analytics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketAnalyticsConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_analytics_configuration(params)
        return result


    async def get_bucket_cors(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketCorsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_cors(params)
        return result


    async def get_bucket_encryption(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketEncryptionOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_encryption(params)
        return result


    async def get_bucket_intelligent_tiering_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketIntelligentTieringConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_intelligent_tiering_configuration(params)
        return result


    async def get_bucket_inventory_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketInventoryConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_inventory_configuration(params)
        return result


    async def get_bucket_lifecycle_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketLifecycleConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_lifecycle_configuration(params)
        return result


    async def get_bucket_location(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketLocationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_location(params)
        return result


    async def get_bucket_logging(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketLoggingOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_logging(params)
        return result


    async def get_bucket_metadata_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketMetadataConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_metadata_configuration(params)
        return result


    async def get_bucket_metadata_table_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketMetadataTableConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_metadata_table_configuration(params)
        return result


    async def get_bucket_metrics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketMetricsConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_metrics_configuration(params)
        return result


    async def get_bucket_notification_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketNotificationConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_notification_configuration(params)
        return result


    async def get_bucket_ownership_controls(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketOwnershipControlsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_ownership_controls(params)
        return result


    async def get_bucket_policy(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketPolicyOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_policy(params)
        return result


    async def get_bucket_policy_status(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketPolicyStatusOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_policy_status(params)
        return result


    async def get_bucket_replication(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketReplicationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_replication(params)
        return result


    async def get_bucket_request_payment(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketRequestPaymentOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_request_payment(params)
        return result


    async def get_bucket_tagging(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketTaggingOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_tagging(params)
        return result


    async def get_bucket_versioning(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketVersioningOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_versioning(params)
        return result


    async def get_bucket_website(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetBucketWebsiteOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_bucket_website(params)
        return result


    async def get_object(
        self,
        *,
        bucket: str,
        key: str,
        if_match: str | None = None,
        if_modified_since: str | None = None,
        if_none_match: str | None = None,
        if_unmodified_since: str | None = None,
        range: str | None = None,
        response_cache_control: str | None = None,
        response_content_disposition: str | None = None,
        response_content_encoding: str | None = None,
        response_content_language: str | None = None,
        response_content_type: str | None = None,
        response_expires: str | None = None,
        version_id: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        part_number: int | None = None,
        expected_bucket_owner: str | None = None,
        checksum_mode: ChecksumMode | None = None,
    ) -> GetObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if if_match is not None:
            params["if_match"] = if_match
        if if_modified_since is not None:
            params["if_modified_since"] = if_modified_since
        if if_none_match is not None:
            params["if_none_match"] = if_none_match
        if if_unmodified_since is not None:
            params["if_unmodified_since"] = if_unmodified_since
        if range is not None:
            params["range"] = range
        if response_cache_control is not None:
            params["response_cache_control"] = response_cache_control
        if response_content_disposition is not None:
            params["response_content_disposition"] = response_content_disposition
        if response_content_encoding is not None:
            params["response_content_encoding"] = response_content_encoding
        if response_content_language is not None:
            params["response_content_language"] = response_content_language
        if response_content_type is not None:
            params["response_content_type"] = response_content_type
        if response_expires is not None:
            params["response_expires"] = response_expires
        if version_id is not None:
            params["version_id"] = version_id
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if part_number is not None:
            params["part_number"] = part_number
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_mode is not None:
            params["checksum_mode"] = checksum_mode
        result = await self._native.get_object(params)
        return result


    async def get_object_acl(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectAclOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_acl(params)
        return result


    async def get_object_attributes(
        self,
        *,
        bucket: str,
        key: str,
        object_attributes: list[ObjectAttributes],
        version_id: str | None = None,
        max_parts: int | None = None,
        part_number_marker: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectAttributesOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "object_attributes": object_attributes,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if max_parts is not None:
            params["max_parts"] = max_parts
        if part_number_marker is not None:
            params["part_number_marker"] = part_number_marker
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_attributes(params)
        return result


    async def get_object_legal_hold(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectLegalHoldOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_legal_hold(params)
        return result


    async def get_object_lock_configuration(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectLockConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_lock_configuration(params)
        return result


    async def get_object_retention(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectRetentionOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_retention(params)
        return result


    async def get_object_tagging(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        expected_bucket_owner: str | None = None,
        request_payer: RequestPayer | None = None,
    ) -> GetObjectTaggingOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if request_payer is not None:
            params["request_payer"] = request_payer
        result = await self._native.get_object_tagging(params)
        return result


    async def get_object_torrent(
        self,
        *,
        bucket: str,
        key: str,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> GetObjectTorrentOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_object_torrent(params)
        return result


    async def get_public_access_block(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> GetPublicAccessBlockOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.get_public_access_block(params)
        return result


    async def head_bucket(
        self,
        *,
        bucket: str,
        expected_bucket_owner: str | None = None,
    ) -> HeadBucketOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.head_bucket(params)
        return result


    async def head_object(
        self,
        *,
        bucket: str,
        key: str,
        if_match: str | None = None,
        if_modified_since: str | None = None,
        if_none_match: str | None = None,
        if_unmodified_since: str | None = None,
        range: str | None = None,
        response_cache_control: str | None = None,
        response_content_disposition: str | None = None,
        response_content_encoding: str | None = None,
        response_content_language: str | None = None,
        response_content_type: str | None = None,
        response_expires: str | None = None,
        version_id: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        part_number: int | None = None,
        expected_bucket_owner: str | None = None,
        checksum_mode: ChecksumMode | None = None,
    ) -> HeadObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if if_match is not None:
            params["if_match"] = if_match
        if if_modified_since is not None:
            params["if_modified_since"] = if_modified_since
        if if_none_match is not None:
            params["if_none_match"] = if_none_match
        if if_unmodified_since is not None:
            params["if_unmodified_since"] = if_unmodified_since
        if range is not None:
            params["range"] = range
        if response_cache_control is not None:
            params["response_cache_control"] = response_cache_control
        if response_content_disposition is not None:
            params["response_content_disposition"] = response_content_disposition
        if response_content_encoding is not None:
            params["response_content_encoding"] = response_content_encoding
        if response_content_language is not None:
            params["response_content_language"] = response_content_language
        if response_content_type is not None:
            params["response_content_type"] = response_content_type
        if response_expires is not None:
            params["response_expires"] = response_expires
        if version_id is not None:
            params["version_id"] = version_id
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if part_number is not None:
            params["part_number"] = part_number
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_mode is not None:
            params["checksum_mode"] = checksum_mode
        result = await self._native.head_object(params)
        return result


    async def list_bucket_analytics_configurations(
        self,
        *,
        bucket: str,
        continuation_token: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> ListBucketAnalyticsConfigurationsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.list_bucket_analytics_configurations(params)
        return result


    async def list_bucket_intelligent_tiering_configurations(
        self,
        *,
        bucket: str,
        continuation_token: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> ListBucketIntelligentTieringConfigurationsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.list_bucket_intelligent_tiering_configurations(params)
        return result


    async def list_bucket_inventory_configurations(
        self,
        *,
        bucket: str,
        continuation_token: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> ListBucketInventoryConfigurationsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.list_bucket_inventory_configurations(params)
        return result


    async def list_bucket_metrics_configurations(
        self,
        *,
        bucket: str,
        continuation_token: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> ListBucketMetricsConfigurationsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.list_bucket_metrics_configurations(params)
        return result


    async def list_buckets(
        self,
        *,
        max_buckets: int | None = None,
        continuation_token: str | None = None,
        prefix: str | None = None,
        bucket_region: str | None = None,
    ) -> ListBucketsOutput:
        params: dict[str, object] = {
        }
        if max_buckets is not None:
            params["max_buckets"] = max_buckets
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if prefix is not None:
            params["prefix"] = prefix
        if bucket_region is not None:
            params["bucket_region"] = bucket_region
        result = await self._native.list_buckets(params)
        return result


    async def list_directory_buckets(
        self,
        *,
        continuation_token: str | None = None,
        max_directory_buckets: int | None = None,
    ) -> ListDirectoryBucketsOutput:
        params: dict[str, object] = {
        }
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if max_directory_buckets is not None:
            params["max_directory_buckets"] = max_directory_buckets
        result = await self._native.list_directory_buckets(params)
        return result


    async def list_multipart_uploads(
        self,
        *,
        bucket: str,
        delimiter: str | None = None,
        encoding_type: EncodingType | None = None,
        key_marker: str | None = None,
        max_uploads: int | None = None,
        prefix: str | None = None,
        upload_id_marker: str | None = None,
        expected_bucket_owner: str | None = None,
        request_payer: RequestPayer | None = None,
    ) -> ListMultipartUploadsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if delimiter is not None:
            params["delimiter"] = delimiter
        if encoding_type is not None:
            params["encoding_type"] = encoding_type
        if key_marker is not None:
            params["key_marker"] = key_marker
        if max_uploads is not None:
            params["max_uploads"] = max_uploads
        if prefix is not None:
            params["prefix"] = prefix
        if upload_id_marker is not None:
            params["upload_id_marker"] = upload_id_marker
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if request_payer is not None:
            params["request_payer"] = request_payer
        result = await self._native.list_multipart_uploads(params)
        return result


    async def list_objects(
        self,
        *,
        bucket: str,
        delimiter: str | None = None,
        encoding_type: EncodingType | None = None,
        marker: str | None = None,
        max_keys: int | None = None,
        prefix: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        optional_object_attributes: list[OptionalObjectAttributes] | None = None,
    ) -> ListObjectsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if delimiter is not None:
            params["delimiter"] = delimiter
        if encoding_type is not None:
            params["encoding_type"] = encoding_type
        if marker is not None:
            params["marker"] = marker
        if max_keys is not None:
            params["max_keys"] = max_keys
        if prefix is not None:
            params["prefix"] = prefix
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if optional_object_attributes is not None:
            params["optional_object_attributes"] = optional_object_attributes
        result = await self._native.list_objects(params)
        return result


    async def list_objects_v2(
        self,
        *,
        bucket: str,
        delimiter: str | None = None,
        encoding_type: EncodingType | None = None,
        max_keys: int | None = None,
        prefix: str | None = None,
        continuation_token: str | None = None,
        fetch_owner: bool | None = None,
        start_after: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        optional_object_attributes: list[OptionalObjectAttributes] | None = None,
    ) -> ListObjectsV2Output:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if delimiter is not None:
            params["delimiter"] = delimiter
        if encoding_type is not None:
            params["encoding_type"] = encoding_type
        if max_keys is not None:
            params["max_keys"] = max_keys
        if prefix is not None:
            params["prefix"] = prefix
        if continuation_token is not None:
            params["continuation_token"] = continuation_token
        if fetch_owner is not None:
            params["fetch_owner"] = fetch_owner
        if start_after is not None:
            params["start_after"] = start_after
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if optional_object_attributes is not None:
            params["optional_object_attributes"] = optional_object_attributes
        result = await self._native.list_objects_v2(params)
        return result


    async def list_object_versions(
        self,
        *,
        bucket: str,
        delimiter: str | None = None,
        encoding_type: EncodingType | None = None,
        key_marker: str | None = None,
        max_keys: int | None = None,
        prefix: str | None = None,
        version_id_marker: str | None = None,
        expected_bucket_owner: str | None = None,
        request_payer: RequestPayer | None = None,
        optional_object_attributes: list[OptionalObjectAttributes] | None = None,
    ) -> ListObjectVersionsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if delimiter is not None:
            params["delimiter"] = delimiter
        if encoding_type is not None:
            params["encoding_type"] = encoding_type
        if key_marker is not None:
            params["key_marker"] = key_marker
        if max_keys is not None:
            params["max_keys"] = max_keys
        if prefix is not None:
            params["prefix"] = prefix
        if version_id_marker is not None:
            params["version_id_marker"] = version_id_marker
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if request_payer is not None:
            params["request_payer"] = request_payer
        if optional_object_attributes is not None:
            params["optional_object_attributes"] = optional_object_attributes
        result = await self._native.list_object_versions(params)
        return result


    async def list_parts(
        self,
        *,
        bucket: str,
        key: str,
        upload_id: str,
        max_parts: int | None = None,
        part_number_marker: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
    ) -> ListPartsOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "upload_id": upload_id,
        }
        if max_parts is not None:
            params["max_parts"] = max_parts
        if part_number_marker is not None:
            params["part_number_marker"] = part_number_marker
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        result = await self._native.list_parts(params)
        return result


    async def put_bucket_abac(
        self,
        *,
        bucket: str,
        abac_status: AbacStatus,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "abac_status": abac_status,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_abac(params)
        return None


    async def put_bucket_accelerate_configuration(
        self,
        *,
        bucket: str,
        accelerate_configuration: AccelerateConfiguration,
        expected_bucket_owner: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "accelerate_configuration": accelerate_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        await self._native.put_bucket_accelerate_configuration(params)
        return None


    async def put_bucket_acl(
        self,
        *,
        bucket: str,
        acl: BucketCannedACL | None = None,
        access_control_policy: AccessControlPolicy | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write: str | None = None,
        grant_write_acp: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if acl is not None:
            params["acl"] = acl
        if access_control_policy is not None:
            params["access_control_policy"] = access_control_policy
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write is not None:
            params["grant_write"] = grant_write
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_acl(params)
        return None


    async def put_bucket_analytics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        analytics_configuration: AnalyticsConfiguration,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
            "analytics_configuration": analytics_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_analytics_configuration(params)
        return None


    async def put_bucket_cors(
        self,
        *,
        bucket: str,
        cors_configuration: CORSConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "cors_configuration": cors_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_cors(params)
        return None


    async def put_bucket_encryption(
        self,
        *,
        bucket: str,
        server_side_encryption_configuration: ServerSideEncryptionConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "server_side_encryption_configuration": server_side_encryption_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_encryption(params)
        return None


    async def put_bucket_intelligent_tiering_configuration(
        self,
        *,
        bucket: str,
        id: str,
        intelligent_tiering_configuration: IntelligentTieringConfiguration,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
            "intelligent_tiering_configuration": intelligent_tiering_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_intelligent_tiering_configuration(params)
        return None


    async def put_bucket_inventory_configuration(
        self,
        *,
        bucket: str,
        id: str,
        inventory_configuration: InventoryConfiguration,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
            "inventory_configuration": inventory_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_inventory_configuration(params)
        return None


    async def put_bucket_lifecycle_configuration(
        self,
        *,
        bucket: str,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        lifecycle_configuration: BucketLifecycleConfiguration | None = None,
        expected_bucket_owner: str | None = None,
        transition_default_minimum_object_size: TransitionDefaultMinimumObjectSize | None = None,
    ) -> PutBucketLifecycleConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if lifecycle_configuration is not None:
            params["lifecycle_configuration"] = lifecycle_configuration
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if transition_default_minimum_object_size is not None:
            params["transition_default_minimum_object_size"] = transition_default_minimum_object_size
        result = await self._native.put_bucket_lifecycle_configuration(params)
        return result


    async def put_bucket_logging(
        self,
        *,
        bucket: str,
        bucket_logging_status: BucketLoggingStatus,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "bucket_logging_status": bucket_logging_status,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_logging(params)
        return None


    async def put_bucket_metrics_configuration(
        self,
        *,
        bucket: str,
        id: str,
        metrics_configuration: MetricsConfiguration,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "id": id,
            "metrics_configuration": metrics_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_metrics_configuration(params)
        return None


    async def put_bucket_notification_configuration(
        self,
        *,
        bucket: str,
        notification_configuration: NotificationConfiguration,
        expected_bucket_owner: str | None = None,
        skip_destination_validation: bool | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "notification_configuration": notification_configuration,
        }
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if skip_destination_validation is not None:
            params["skip_destination_validation"] = skip_destination_validation
        await self._native.put_bucket_notification_configuration(params)
        return None


    async def put_bucket_ownership_controls(
        self,
        *,
        bucket: str,
        ownership_controls: OwnershipControls,
        content_md5: str | None = None,
        expected_bucket_owner: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "ownership_controls": ownership_controls,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        await self._native.put_bucket_ownership_controls(params)
        return None


    async def put_bucket_policy(
        self,
        *,
        bucket: str,
        policy: str,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        confirm_remove_self_bucket_access: bool | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "policy": policy,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if confirm_remove_self_bucket_access is not None:
            params["confirm_remove_self_bucket_access"] = confirm_remove_self_bucket_access
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_policy(params)
        return None


    async def put_bucket_replication(
        self,
        *,
        bucket: str,
        replication_configuration: ReplicationConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        token: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "replication_configuration": replication_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if token is not None:
            params["token"] = token
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_replication(params)
        return None


    async def put_bucket_request_payment(
        self,
        *,
        bucket: str,
        request_payment_configuration: RequestPaymentConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "request_payment_configuration": request_payment_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_request_payment(params)
        return None


    async def put_bucket_tagging(
        self,
        *,
        bucket: str,
        tagging: Tagging,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "tagging": tagging,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_tagging(params)
        return None


    async def put_bucket_versioning(
        self,
        *,
        bucket: str,
        versioning_configuration: VersioningConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        mfa: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "versioning_configuration": versioning_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if mfa is not None:
            params["mfa"] = mfa
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_versioning(params)
        return None


    async def put_bucket_website(
        self,
        *,
        bucket: str,
        website_configuration: WebsiteConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "website_configuration": website_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_bucket_website(params)
        return None


    async def put_object(
        self,
        *,
        bucket: str,
        key: str,
        acl: ObjectCannedACL | None = None,
        body: bytes | None = None,
        cache_control: str | None = None,
        content_disposition: str | None = None,
        content_encoding: str | None = None,
        content_language: str | None = None,
        content_length: int | None = None,
        content_md5: str | None = None,
        content_type: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        checksum_crc32: str | None = None,
        checksum_crc32_c: str | None = None,
        checksum_crc64_nvme: str | None = None,
        checksum_sha1: str | None = None,
        checksum_sha256: str | None = None,
        expires: str | None = None,
        if_match: str | None = None,
        if_none_match: str | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write_acp: str | None = None,
        write_offset_bytes: int | None = None,
        metadata: dict[str, str] | None = None,
        server_side_encryption: ServerSideEncryption | None = None,
        storage_class: StorageClass | None = None,
        website_redirect_location: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        ssekms_key_id: str | None = None,
        ssekms_encryption_context: str | None = None,
        bucket_key_enabled: bool | None = None,
        request_payer: RequestPayer | None = None,
        tagging: str | None = None,
        object_lock_mode: ObjectLockMode | None = None,
        object_lock_retain_until_date: str | None = None,
        object_lock_legal_hold_status: ObjectLockLegalHoldStatus | None = None,
        expected_bucket_owner: str | None = None,
    ) -> PutObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if acl is not None:
            params["acl"] = acl
        if body is not None:
            params["body"] = body
        if cache_control is not None:
            params["cache_control"] = cache_control
        if content_disposition is not None:
            params["content_disposition"] = content_disposition
        if content_encoding is not None:
            params["content_encoding"] = content_encoding
        if content_language is not None:
            params["content_language"] = content_language
        if content_length is not None:
            params["content_length"] = content_length
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if content_type is not None:
            params["content_type"] = content_type
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if checksum_crc32 is not None:
            params["checksum_crc32"] = checksum_crc32
        if checksum_crc32_c is not None:
            params["checksum_crc32_c"] = checksum_crc32_c
        if checksum_crc64_nvme is not None:
            params["checksum_crc64_nvme"] = checksum_crc64_nvme
        if checksum_sha1 is not None:
            params["checksum_sha1"] = checksum_sha1
        if checksum_sha256 is not None:
            params["checksum_sha256"] = checksum_sha256
        if expires is not None:
            params["expires"] = expires
        if if_match is not None:
            params["if_match"] = if_match
        if if_none_match is not None:
            params["if_none_match"] = if_none_match
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if write_offset_bytes is not None:
            params["write_offset_bytes"] = write_offset_bytes
        if metadata is not None:
            params["metadata"] = metadata
        if server_side_encryption is not None:
            params["server_side_encryption"] = server_side_encryption
        if storage_class is not None:
            params["storage_class"] = storage_class
        if website_redirect_location is not None:
            params["website_redirect_location"] = website_redirect_location
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if ssekms_key_id is not None:
            params["ssekms_key_id"] = ssekms_key_id
        if ssekms_encryption_context is not None:
            params["ssekms_encryption_context"] = ssekms_encryption_context
        if bucket_key_enabled is not None:
            params["bucket_key_enabled"] = bucket_key_enabled
        if request_payer is not None:
            params["request_payer"] = request_payer
        if tagging is not None:
            params["tagging"] = tagging
        if object_lock_mode is not None:
            params["object_lock_mode"] = object_lock_mode
        if object_lock_retain_until_date is not None:
            params["object_lock_retain_until_date"] = object_lock_retain_until_date
        if object_lock_legal_hold_status is not None:
            params["object_lock_legal_hold_status"] = object_lock_legal_hold_status
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.put_object(params)
        return result


    async def put_object_acl(
        self,
        *,
        bucket: str,
        key: str,
        acl: ObjectCannedACL | None = None,
        access_control_policy: AccessControlPolicy | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        grant_full_control: str | None = None,
        grant_read: str | None = None,
        grant_read_acp: str | None = None,
        grant_write: str | None = None,
        grant_write_acp: str | None = None,
        request_payer: RequestPayer | None = None,
        version_id: str | None = None,
        expected_bucket_owner: str | None = None,
    ) -> PutObjectAclOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if acl is not None:
            params["acl"] = acl
        if access_control_policy is not None:
            params["access_control_policy"] = access_control_policy
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if grant_full_control is not None:
            params["grant_full_control"] = grant_full_control
        if grant_read is not None:
            params["grant_read"] = grant_read
        if grant_read_acp is not None:
            params["grant_read_acp"] = grant_read_acp
        if grant_write is not None:
            params["grant_write"] = grant_write
        if grant_write_acp is not None:
            params["grant_write_acp"] = grant_write_acp
        if request_payer is not None:
            params["request_payer"] = request_payer
        if version_id is not None:
            params["version_id"] = version_id
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.put_object_acl(params)
        return result


    async def put_object_legal_hold(
        self,
        *,
        bucket: str,
        key: str,
        legal_hold: ObjectLockLegalHold | None = None,
        request_payer: RequestPayer | None = None,
        version_id: str | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> PutObjectLegalHoldOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if legal_hold is not None:
            params["legal_hold"] = legal_hold
        if request_payer is not None:
            params["request_payer"] = request_payer
        if version_id is not None:
            params["version_id"] = version_id
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.put_object_legal_hold(params)
        return result


    async def put_object_lock_configuration(
        self,
        *,
        bucket: str,
        object_lock_configuration: ObjectLockConfiguration | None = None,
        request_payer: RequestPayer | None = None,
        token: str | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> PutObjectLockConfigurationOutput:
        params: dict[str, object] = {
            "bucket": bucket,
        }
        if object_lock_configuration is not None:
            params["object_lock_configuration"] = object_lock_configuration
        if request_payer is not None:
            params["request_payer"] = request_payer
        if token is not None:
            params["token"] = token
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.put_object_lock_configuration(params)
        return result


    async def put_object_retention(
        self,
        *,
        bucket: str,
        key: str,
        retention: ObjectLockRetention | None = None,
        request_payer: RequestPayer | None = None,
        version_id: str | None = None,
        bypass_governance_retention: bool | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> PutObjectRetentionOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if retention is not None:
            params["retention"] = retention
        if request_payer is not None:
            params["request_payer"] = request_payer
        if version_id is not None:
            params["version_id"] = version_id
        if bypass_governance_retention is not None:
            params["bypass_governance_retention"] = bypass_governance_retention
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.put_object_retention(params)
        return result


    async def put_object_tagging(
        self,
        *,
        bucket: str,
        key: str,
        tagging: Tagging,
        version_id: str | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
        request_payer: RequestPayer | None = None,
    ) -> PutObjectTaggingOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "tagging": tagging,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if request_payer is not None:
            params["request_payer"] = request_payer
        result = await self._native.put_object_tagging(params)
        return result


    async def put_public_access_block(
        self,
        *,
        bucket: str,
        public_access_block_configuration: PublicAccessBlockConfiguration,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "public_access_block_configuration": public_access_block_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.put_public_access_block(params)
        return None


    async def rename_object(
        self,
        *,
        bucket: str,
        key: str,
        rename_source: str,
        destination_if_match: str | None = None,
        destination_if_none_match: str | None = None,
        destination_if_modified_since: str | None = None,
        destination_if_unmodified_since: str | None = None,
        source_if_match: str | None = None,
        source_if_none_match: str | None = None,
        source_if_modified_since: str | None = None,
        source_if_unmodified_since: str | None = None,
        client_token: str | None = None,
    ) -> RenameObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "rename_source": rename_source,
        }
        if destination_if_match is not None:
            params["destination_if_match"] = destination_if_match
        if destination_if_none_match is not None:
            params["destination_if_none_match"] = destination_if_none_match
        if destination_if_modified_since is not None:
            params["destination_if_modified_since"] = destination_if_modified_since
        if destination_if_unmodified_since is not None:
            params["destination_if_unmodified_since"] = destination_if_unmodified_since
        if source_if_match is not None:
            params["source_if_match"] = source_if_match
        if source_if_none_match is not None:
            params["source_if_none_match"] = source_if_none_match
        if source_if_modified_since is not None:
            params["source_if_modified_since"] = source_if_modified_since
        if source_if_unmodified_since is not None:
            params["source_if_unmodified_since"] = source_if_unmodified_since
        if client_token is not None:
            params["client_token"] = client_token
        result = await self._native.rename_object(params)
        return result


    async def restore_object(
        self,
        *,
        bucket: str,
        key: str,
        version_id: str | None = None,
        restore_request: RestoreRequest | None = None,
        request_payer: RequestPayer | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> RestoreObjectOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
        }
        if version_id is not None:
            params["version_id"] = version_id
        if restore_request is not None:
            params["restore_request"] = restore_request
        if request_payer is not None:
            params["request_payer"] = request_payer
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.restore_object(params)
        return result


    async def select_object_content(
        self,
        *,
        bucket: str,
        key: str,
        expression: str,
        expression_type: ExpressionType,
        input_serialization: InputSerialization,
        output_serialization: OutputSerialization,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        request_progress: RequestProgress | None = None,
        scan_range: ScanRange | None = None,
        expected_bucket_owner: str | None = None,
    ) -> SelectObjectContentOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "expression": expression,
            "expression_type": expression_type,
            "input_serialization": input_serialization,
            "output_serialization": output_serialization,
        }
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if request_progress is not None:
            params["request_progress"] = request_progress
        if scan_range is not None:
            params["scan_range"] = scan_range
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.select_object_content(params)
        return result


    async def update_bucket_metadata_inventory_table_configuration(
        self,
        *,
        bucket: str,
        inventory_table_configuration: InventoryTableConfigurationUpdates,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "inventory_table_configuration": inventory_table_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.update_bucket_metadata_inventory_table_configuration(params)
        return None


    async def update_bucket_metadata_journal_table_configuration(
        self,
        *,
        bucket: str,
        journal_table_configuration: JournalTableConfigurationUpdates,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        expected_bucket_owner: str | None = None,
    ) -> None:
        params: dict[str, object] = {
            "bucket": bucket,
            "journal_table_configuration": journal_table_configuration,
        }
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        await self._native.update_bucket_metadata_journal_table_configuration(params)
        return None


    async def upload_part(
        self,
        *,
        bucket: str,
        key: str,
        part_number: int,
        upload_id: str,
        body: bytes | None = None,
        content_length: int | None = None,
        content_md5: str | None = None,
        checksum_algorithm: ChecksumAlgorithm | None = None,
        checksum_crc32: str | None = None,
        checksum_crc32_c: str | None = None,
        checksum_crc64_nvme: str | None = None,
        checksum_sha1: str | None = None,
        checksum_sha256: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
    ) -> UploadPartOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "key": key,
            "part_number": part_number,
            "upload_id": upload_id,
        }
        if body is not None:
            params["body"] = body
        if content_length is not None:
            params["content_length"] = content_length
        if content_md5 is not None:
            params["content_md5"] = content_md5
        if checksum_algorithm is not None:
            params["checksum_algorithm"] = checksum_algorithm
        if checksum_crc32 is not None:
            params["checksum_crc32"] = checksum_crc32
        if checksum_crc32_c is not None:
            params["checksum_crc32_c"] = checksum_crc32_c
        if checksum_crc64_nvme is not None:
            params["checksum_crc64_nvme"] = checksum_crc64_nvme
        if checksum_sha1 is not None:
            params["checksum_sha1"] = checksum_sha1
        if checksum_sha256 is not None:
            params["checksum_sha256"] = checksum_sha256
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        result = await self._native.upload_part(params)
        return result


    async def upload_part_copy(
        self,
        *,
        bucket: str,
        copy_source: str,
        key: str,
        part_number: int,
        upload_id: str,
        copy_source_if_match: str | None = None,
        copy_source_if_modified_since: str | None = None,
        copy_source_if_none_match: str | None = None,
        copy_source_if_unmodified_since: str | None = None,
        copy_source_range: str | None = None,
        sse_customer_algorithm: str | None = None,
        sse_customer_key: str | None = None,
        sse_customer_key_md5: str | None = None,
        copy_source_sse_customer_algorithm: str | None = None,
        copy_source_sse_customer_key: str | None = None,
        copy_source_sse_customer_key_md5: str | None = None,
        request_payer: RequestPayer | None = None,
        expected_bucket_owner: str | None = None,
        expected_source_bucket_owner: str | None = None,
    ) -> UploadPartCopyOutput:
        params: dict[str, object] = {
            "bucket": bucket,
            "copy_source": copy_source,
            "key": key,
            "part_number": part_number,
            "upload_id": upload_id,
        }
        if copy_source_if_match is not None:
            params["copy_source_if_match"] = copy_source_if_match
        if copy_source_if_modified_since is not None:
            params["copy_source_if_modified_since"] = copy_source_if_modified_since
        if copy_source_if_none_match is not None:
            params["copy_source_if_none_match"] = copy_source_if_none_match
        if copy_source_if_unmodified_since is not None:
            params["copy_source_if_unmodified_since"] = copy_source_if_unmodified_since
        if copy_source_range is not None:
            params["copy_source_range"] = copy_source_range
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if sse_customer_key is not None:
            params["sse_customer_key"] = sse_customer_key
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if copy_source_sse_customer_algorithm is not None:
            params["copy_source_sse_customer_algorithm"] = copy_source_sse_customer_algorithm
        if copy_source_sse_customer_key is not None:
            params["copy_source_sse_customer_key"] = copy_source_sse_customer_key
        if copy_source_sse_customer_key_md5 is not None:
            params["copy_source_sse_customer_key_md5"] = copy_source_sse_customer_key_md5
        if request_payer is not None:
            params["request_payer"] = request_payer
        if expected_bucket_owner is not None:
            params["expected_bucket_owner"] = expected_bucket_owner
        if expected_source_bucket_owner is not None:
            params["expected_source_bucket_owner"] = expected_source_bucket_owner
        result = await self._native.upload_part_copy(params)
        return result


    async def write_get_object_response(
        self,
        *,
        request_route: str,
        request_token: str,
        body: bytes | None = None,
        status_code: int | None = None,
        error_code: str | None = None,
        error_message: str | None = None,
        accept_ranges: str | None = None,
        cache_control: str | None = None,
        content_disposition: str | None = None,
        content_encoding: str | None = None,
        content_language: str | None = None,
        content_length: int | None = None,
        content_range: str | None = None,
        content_type: str | None = None,
        checksum_crc32: str | None = None,
        checksum_crc32_c: str | None = None,
        checksum_crc64_nvme: str | None = None,
        checksum_sha1: str | None = None,
        checksum_sha256: str | None = None,
        delete_marker: bool | None = None,
        e_tag: str | None = None,
        expires: str | None = None,
        expiration: str | None = None,
        last_modified: str | None = None,
        missing_meta: int | None = None,
        metadata: dict[str, str] | None = None,
        object_lock_mode: ObjectLockMode | None = None,
        object_lock_legal_hold_status: ObjectLockLegalHoldStatus | None = None,
        object_lock_retain_until_date: str | None = None,
        parts_count: int | None = None,
        replication_status: ReplicationStatus | None = None,
        request_charged: RequestCharged | None = None,
        restore: str | None = None,
        server_side_encryption: ServerSideEncryption | None = None,
        sse_customer_algorithm: str | None = None,
        ssekms_key_id: str | None = None,
        sse_customer_key_md5: str | None = None,
        storage_class: StorageClass | None = None,
        tag_count: int | None = None,
        version_id: str | None = None,
        bucket_key_enabled: bool | None = None,
    ) -> None:
        params: dict[str, object] = {
            "request_route": request_route,
            "request_token": request_token,
        }
        if body is not None:
            params["body"] = body
        if status_code is not None:
            params["status_code"] = status_code
        if error_code is not None:
            params["error_code"] = error_code
        if error_message is not None:
            params["error_message"] = error_message
        if accept_ranges is not None:
            params["accept_ranges"] = accept_ranges
        if cache_control is not None:
            params["cache_control"] = cache_control
        if content_disposition is not None:
            params["content_disposition"] = content_disposition
        if content_encoding is not None:
            params["content_encoding"] = content_encoding
        if content_language is not None:
            params["content_language"] = content_language
        if content_length is not None:
            params["content_length"] = content_length
        if content_range is not None:
            params["content_range"] = content_range
        if content_type is not None:
            params["content_type"] = content_type
        if checksum_crc32 is not None:
            params["checksum_crc32"] = checksum_crc32
        if checksum_crc32_c is not None:
            params["checksum_crc32_c"] = checksum_crc32_c
        if checksum_crc64_nvme is not None:
            params["checksum_crc64_nvme"] = checksum_crc64_nvme
        if checksum_sha1 is not None:
            params["checksum_sha1"] = checksum_sha1
        if checksum_sha256 is not None:
            params["checksum_sha256"] = checksum_sha256
        if delete_marker is not None:
            params["delete_marker"] = delete_marker
        if e_tag is not None:
            params["e_tag"] = e_tag
        if expires is not None:
            params["expires"] = expires
        if expiration is not None:
            params["expiration"] = expiration
        if last_modified is not None:
            params["last_modified"] = last_modified
        if missing_meta is not None:
            params["missing_meta"] = missing_meta
        if metadata is not None:
            params["metadata"] = metadata
        if object_lock_mode is not None:
            params["object_lock_mode"] = object_lock_mode
        if object_lock_legal_hold_status is not None:
            params["object_lock_legal_hold_status"] = object_lock_legal_hold_status
        if object_lock_retain_until_date is not None:
            params["object_lock_retain_until_date"] = object_lock_retain_until_date
        if parts_count is not None:
            params["parts_count"] = parts_count
        if replication_status is not None:
            params["replication_status"] = replication_status
        if request_charged is not None:
            params["request_charged"] = request_charged
        if restore is not None:
            params["restore"] = restore
        if server_side_encryption is not None:
            params["server_side_encryption"] = server_side_encryption
        if sse_customer_algorithm is not None:
            params["sse_customer_algorithm"] = sse_customer_algorithm
        if ssekms_key_id is not None:
            params["ssekms_key_id"] = ssekms_key_id
        if sse_customer_key_md5 is not None:
            params["sse_customer_key_md5"] = sse_customer_key_md5
        if storage_class is not None:
            params["storage_class"] = storage_class
        if tag_count is not None:
            params["tag_count"] = tag_count
        if version_id is not None:
            params["version_id"] = version_id
        if bucket_key_enabled is not None:
            params["bucket_key_enabled"] = bucket_key_enabled
        await self._native.write_get_object_response(params)
        return None

